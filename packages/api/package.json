{
  "files": [
    "src",
    "!src/**/!(*.d).ts"
  ],
  "license": "MIT",
  "main": "src/index",
  "name": "@tstv/api",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf src/**/*{.js,.js.map,.d.ts}",
    "build": "tsc -p tsconfig.build.json",
    "dist": "yarn clean && yarn build"
  },
  "version": "0.0.0"
}
